**原理概述：** 
1. 数据压缩：clickhouse的海量数据存储和快速数据查询&分析能力主要来源于clickhouse强大的数据压缩能力，所以开发者要熟悉clickhouse的数据存储压缩算法，针对不同的存储类型和数据的特性使用不同的数据压缩法。[[ClickHouse数据压缩]]
2. 索引优化：[[ClickHouse索引优化]]
3. 分区设置（partition）+ TTL : clickhouse自带数据按照时间分区，无需手动分库分表，且给数据增加TTL，比如当数据存储超过特定时间后自动迁移到冷盘或者定期淘汰。
4. 磁盘IO顺序读写：监控数据的写入可以按照时间顺序写入磁盘，读取的时候由于监控数据是根据时间进行的partition分区，所以读取数据与写入顺序一致，会大幅度提升数据读取的性能。


