线性回归的训练思路

1. 假设模型：$y1 = wx+b$
    
2. 损失函数：$L=∑(y−y1)^2$ (损失即所有点到原函数的距离平方)
    
3. 求导：对 w 和 b 求[[偏导数]]
    
4. [[梯度下降]]：沿着**减小损失的方向**调整 w 和 b，即求L对于w的偏导数
	如果：$\frac{\partial L}{\partial w} > 0$
	说明：**w 增大，L 变大（往右是上坡）**
5. 收敛后，得到“最优直线”