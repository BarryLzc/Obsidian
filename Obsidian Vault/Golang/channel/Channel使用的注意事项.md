## 1. Channel的关闭要由发送者决定
由发送者关闭会造成channel panic：
![[消费者关闭channel.png]]

## 2. 缓冲区满了后可能的异常
消费者启动晚于生产者，缓冲队列满后导致死锁：
![[消费者启动需先于发送者.png]]


## 小节
**先启动消费者，由发送者关闭channel**
![[正确的使用.png]]

**使用select实现非阻塞发送**
![[非阻塞发送.png]]

**设置超时时间，消费者非阻塞不等待**
结果会打印v的值和default
![[设置超时.png]]

**设置超时时间，消费者阻塞等待**
![[阻塞等待消费.png]]