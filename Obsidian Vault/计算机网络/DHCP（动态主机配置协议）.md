1. 无效地址：windows系统中：169.254.x.x，linx系统中：0.0.00无效地址
2. IP分配的方式
- 固定分配（管理员分配静态绑定固定的IP地址）
- 动态分配（为客户端分配租期为无限长的IP地址）
- 自动分配（为客户端分配具有一定有效期限的IP地址）
3. 下列关于DHCP服务说法正确的是
- 在DHCP服务器上，DHCP服务功能默认关闭
- 默认情况下，DHCP服务器可以管理多个网关，客户端可以不选择本网段内的IP地址
- 默认情况下，下午段使用DHCP服务不需要指定DHCP服务地址
- 一个园区网内可以存在多台DHCP服务器